<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Assist Mode / Scan: on-device obstacle detection + sign reading -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Supabase (auth + crowdsourced maps) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Nearby buildings, and the place name on Home. Coarse is enough: the
         index sorts buildings by kilometres, and asking for fine location to
         answer "which campus am I on" is more than the feature needs. Fine is
         declared too because Android grants coarse-only from the same dialog
         and geolocator asks for whichever is available. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <!-- Guidance: hardware step counter, for distance between landmarks.
         Runtime-dangerous from API 29, so it is also requested through
         permission_handler; declaring it alone is not enough. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <!-- Like ARCore above, declared OPTIONAL on purpose: some budget and
         Samsung devices expose no step counter at all, and requiring one
         would lock exactly this project's target users out of an app whose
         OCR-only fallback (spec §7 B5 rung 2) works without it. -->
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false"/>
    <uses-feature android:name="android.hardware.sensor.stepdetector" android:required="false"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <!-- M0: ARCore depth scanning. Declared OPTIONAL on purpose.
         "required" would make the app un-installable from Play on every
         uncertified device, which for an accessibility app aimed partly at
         users on budget hardware would lock them out of sonar, browsing and
         navigation too — none of which need ARCore. Scanning is instead
         gated at runtime (ArCoreDepthHandler.checkAvailability). -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false"/>

    <application
        android:label="echo_locate"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- "optional" pairs with the uses-feature above: Play may install
             ARCore where supported, but its absence is not an install
             blocker. "required" here would defeat the point. -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
